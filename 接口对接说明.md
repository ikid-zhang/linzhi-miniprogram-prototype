# 户政户籍业务小程序 · 接口对接说明

> 本说明整理自《户政户籍业务表单与审核流程梳理》及「互联网+」接口文档，供开发对接使用；小程序原型页面中已隐藏以下内部对接信息。

## 一、总体对接流程

```
小程序端                互联网+接口（rkxt）           户籍系统
   │                           │                       │
   ├─ 用户填写业务表单 ──────────►   POST JSON           │
   │                           │  SM2 加密推送 ─────────► 受理
   │                           │                       ├─ 审核
   │                           │                       └─ 办结/退回
   │  ◄──── 回调通知（SM2 加密）─┘                       │
   ├─ 进度展示 / 状态更新                                │
   └─ 办结后生成电子证明 → 「亮码服务」出示（二维码核验）
```

- **接口基址**：`http://ip:port/rkxt`（「互联网+」对接基址，正式环境以部署配置为准）
- **请求方式**：POST，JSON 报文
- **加密方式**：报文整体采用 **SM2 国密算法** 加密后推送

## 二、业务接口清单

| 业务 | 接口路径 | 关键参数 | 说明 |
| --- | --- | --- | --- |
| 出生登记 | `/csdj` | `sqlx=0101`，`ywly=9` | 新生儿入户、收养登记、国外出生子女落户 |
| 户籍证明 | `/dysq` | `dyzl=10`，`ysqrgx_jtgxdm=01`（本人） | 户口登记项目变更更正证明、亲属关系证明等 |
| 主项变更更正 | `/bggzsq` | `bggzsjxbsf`（变更数据项标识符） | 姓名、性别、出生日期、民族、身份证号等主项 |
| 户籍补录 | `/bulu` | — | 无户口人员补登补录、漏登补录 |
| 户籍迁移 | `/swqr` | `qyldyydm`（迁移原因代码） | 夫妻/父母子女投靠、购房落户、工作调动、人才引进、大中专录取毕业等 |

## 三、回调协议

户籍系统完成审批后回调小程序，携带字段：

| 字段 | 说明 |
| --- | --- |
| `timeStamp` | 回调时间戳 |
| `appId` | 应用标识 |
| `parameter` | SM2 加密的业务结果报文 |

`parameter` 解密后包含：

| 字段 | 说明 |
| --- | --- |
| `status` | 办理状态（见下表） |
| `result` | 办理结果 / 电子证照数据 |
| `opinion` | 审批意见（退回时必填） |
| `checkTime` | 审批时间 |
| `checkUserId` | 审批民警用户 ID |
| `deptId` | 审批部门 ID |
| `orgName` | 审批机关名称 |
| `fileList` | 电子证照 / 附件文件列表 |

**status 状态码：**

| 状态码 | 含义 | 小程序侧处理 |
| --- | --- | --- |
| `2` | 预审通过 | 展示"预审通过"，等待正式办理 |
| `3` | 预审未通过 | 展示退回原因，允许补充材料重新提交 |
| `9` | 已办理 | 生成电子证明，推送至「亮码服务」，**默认短信通知申请人** |
| `13` | 未通过 | 展示审批意见，告知申请人 |

## 四、审批层级与时限（供业务口径参考）

| 业务 | 审批层级 | 办理时限 |
| --- | --- | --- |
| 出生登记 | 6 周岁以下当场办结；6 周岁以上需社区民警调查，县局/市局审批 | 材料齐全当场 / 按调查流程 |
| 户籍证明 | 户籍地申请当场出具；异地申请需身份查证后反馈 | 当场 / 异地 5 个工作日 |
| 主项变更更正 | 姓名、性别、民族：县局核准；出生日期：市局决定；副项当场办结 | 县局 3–15 个工作日；市局 5–18 个工作日 |
| 户籍补录 | 市局审批（含社区民警走访核实、DNA 打拐库比对） | 按调查审批流程 |
| 户籍迁移 | 省内网上迁移当场办理；跨省通办由迁入地派出所一站式办理 | 当场 / 通办流程 |

## 五、电子证明与短信通知

1. **证明生成**：`status=9` 办结后，户籍系统在 `fileList` 返回电子证照（户籍证明、出生医学证明等），小程序侧存入「亮码服务」（我的证照）对应分类。
2. **短信通知**：办结回调成功入库后，**默认向申请人手机号发送短信**，提示办理结果及亮码查看方式。
3. **亮码核验**：申请人向办事窗口出示二维码，扫码核验电子证明真伪，与纸质证明同等效力。

## 六、对接注意事项

- 所有请求报文必须先做 SM2 加密再发送，回调报文先解密再解析，私钥/公钥由双方约定。
- 各业务表单字段以《户政户籍业务表单与审核流程梳理》为准，推送字段与表单字段一一对应。
- 回调需做幂等处理（以申请单号为准），避免重复办结重复通知。
- 电子证照文件（`fileList`）建议做本地缓存，离线亮码可读。
